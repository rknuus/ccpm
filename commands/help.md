---
allowed-tools: Bash(bash ${CLAUDE_PLUGIN_ROOT}/scripts/pm/help.sh)
---

**NOTE:** `/ccpm:init` is optional. If the user has not run it, commands will still work — init only pre-creates the `.pm/` directory structure.

Output:
!bash ${CLAUDE_PLUGIN_ROOT}/scripts/pm/help.sh
